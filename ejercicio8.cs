using System.Collections.Generic;
using UnityEngine;

public class ejercicio8 : MonoBehaviour
{
    
    List<int> lista = new List<int> { 1, 2, 3, 4 };
    void Start()
    {
        print("lista numero : " + lista[0]);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
